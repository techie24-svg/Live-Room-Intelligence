# Delta: Restore Participant UI + Keep Name Feature

This delta replaces only:

- `app/room/[code]/page.jsx`

It restores the original participant page layout and adds a small inline name/nickname field inside the room header.

## Apply

1. Copy the files into your repo.
2. Commit and push.
3. Redeploy Vercel with build cache off.
