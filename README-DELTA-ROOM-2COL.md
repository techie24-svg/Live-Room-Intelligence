# Delta: Participant Room Two-Column Desktop Layout

Copy these files into your existing project and overwrite matching paths.

Changed:
- `app/room/[code]/page.jsx`

What changed:
- Participant page stays stacked on mobile.
- On desktop, it becomes two columns like the host dashboard.
- Left column: room details + feeling vote.
- Right column: ask question + my questions.
- My questions section scrolls instead of making the page very long.

After copying:
1. Commit to GitHub.
2. Redeploy on Vercel with build cache off.
